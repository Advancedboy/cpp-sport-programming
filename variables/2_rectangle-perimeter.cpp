#include <iostream> // В этой задаче требуется найти периметр прямоугольника, зная его стороны

using namespace std;

int main(){
    int a, b;
    cin >> a;
    cin >> b;
    cout << (2 * (a + b));
    return 0;
}
